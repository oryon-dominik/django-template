# -- Modular settings ---------------------------------------------------------
# These settings are imported in the __init__.py of the base directory explicitly.
# To add a new setting, also edit the __init__.py
# -----------------------------------------------------------------------------
from .. import paths

DJANGO_VITE_ASSETS_PATH = paths.ROOT_DIR / "assets" / "dist"
# DJANGO_VITE_ASSETS_PATH = ROOT_DIR / "apps" / "vueindjango" / "assets" / "src"
DJANGO_VITE_DEV_SERVER_PORT = 5173
DJANGO_VITE_DEV_MODE = True
