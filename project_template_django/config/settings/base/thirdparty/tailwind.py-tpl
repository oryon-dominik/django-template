import sys

from ..paths import ROOT_DIR


# Is a folder to download to, or the preinstalled path to the preinstalled tailwind cli.
TAILWIND_CLI_PATH = "~/scoop/shims/tailwindcss.exe" if sys.platform == "win32" else "tailwindcli"
TAILWIND_CLI_CONFIG_DIR = ROOT_DIR / "frontend" / "django-tailwind-cli"
TAILWIND_CLI_CONFIG_DIR.mkdir(exist_ok=True)
TAILWIND_CLI_CONFIG_FILE = TAILWIND_CLI_CONFIG_DIR / "tailwind.config.js"
TAILWIND_CLI_AUTOMATIC_DOWNLOAD = False
