from django.db import models

from core.models.users import CoreBaseUser


class User(CoreBaseUser):

    class Roles(models.TextChoices):
        # roles derived from CoreBaseUser.Roles:
        INCOMPLETE = "inc", "Incomplete Profile"  # !default
        ADMIN = "adm", "Admin"  # dev only
        API = "api", "Api User"
        # additional roles
        # ...
